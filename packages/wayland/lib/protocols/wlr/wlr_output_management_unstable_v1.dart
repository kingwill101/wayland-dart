// Generated by dart-wayland-scanner
// https://github.com/your-repo/dart-wayland-scanner
// XML file : https://gitlab.freedesktop.org/wlroots/wlr-protocols/-/raw/master/unstable/wlr-output-management-unstable-v1.xml
//
// wlr_output_management_unstable_v1 Protocol Copyright: 
/// 
/// Copyright © 2019 Purism SPC
/// 
/// Permission to use, copy, modify, distribute, and sell this
/// software and its documentation for any purpose is hereby granted
/// without fee, provided that the above copyright notice appear in
/// all copies and that both that copyright notice and this permission
/// notice appear in supporting documentation, and that the name of
/// the copyright holders not be used in advertising or publicity
/// pertaining to distribution of the software without specific,
/// written prior permission.  The copyright holders make no
/// representations about the suitability of this software for any
/// purpose.  It is provided "as is" without express or implied
/// warranty.
/// 
/// THE COPYRIGHT HOLDERS DISCLAIM ALL WARRANTIES WITH REGARD TO THIS
/// SOFTWARE, INCLUDING ALL IMPLIED WARRANTIES OF MERCHANTABILITY AND
/// FITNESS, IN NO EVENT SHALL THE COPYRIGHT HOLDERS BE LIABLE FOR ANY
/// SPECIAL, INDIRECT OR CONSEQUENTIAL DAMAGES OR ANY DAMAGES
/// WHATSOEVER RESULTING FROM LOSS OF USE, DATA OR PROFITS, WHETHER IN
/// AN ACTION OF CONTRACT, NEGLIGENCE OR OTHER TORTIOUS ACTION,
/// ARISING OUT OF OR IN CONNECTION WITH THE USE OR PERFORMANCE OF
/// THIS SOFTWARE.
/// 

library wayland;

import 'package:wayland/wayland.dart';
import 'package:wayland/protocols/wayland.dart';
import 'dart:async';
import 'dart:convert';
import 'dart:typed_data';
import 'package:result_dart/result_dart.dart';

/// introduce a new head
/// 
/// This event introduces a new head. This happens whenever a new head
/// appears (e.g. a monitor is plugged in) or after the output manager is
/// bound.
/// 
class OutputManagerV1HeadEvent {
/// 
  final int head;

  OutputManagerV1HeadEvent(
this.head,

);

@override
toString(){
return "OutputManagerV1HeadEvent (head: $head)";
}
}

typedef OutputManagerV1HeadEventHandler = void Function(OutputManagerV1HeadEvent);

/// sent all information about current configuration
/// 
/// This event is sent after all information has been sent after binding to
/// the output manager object and after any subsequent changes. This applies
/// to child head and mode objects as well. In other words, this event is
/// sent whenever a head or mode is created or destroyed and whenever one of
/// their properties has been changed. Not all state is re-sent each time
/// the current configuration changes: only the actual changes are sent.
/// 
/// This allows changes to the output configuration to be seen as atomic,
/// even if they happen via multiple events.
/// 
/// A serial is sent to be used in a future create_configuration request.
/// 
class OutputManagerV1DoneEvent {
/// current configuration serial
  final int serial;

  OutputManagerV1DoneEvent(
this.serial,

);

@override
toString(){
return "OutputManagerV1DoneEvent (serial: $serial)";
}
}

typedef OutputManagerV1DoneEventHandler = void Function(OutputManagerV1DoneEvent);

/// the compositor has finished with the manager
/// 
/// This event indicates that the compositor is done sending manager events.
/// The compositor will destroy the object immediately after sending this
/// event, so it will become invalid and the client should release any
/// resources associated with it.
/// 
class OutputManagerV1FinishedEvent {
  OutputManagerV1FinishedEvent(
);

@override
toString(){
return "OutputManagerV1FinishedEvent ()";
}
}

typedef OutputManagerV1FinishedEventHandler = void Function(OutputManagerV1FinishedEvent);


/// output device configuration manager
/// 
/// This interface is a manager that allows reading and writing the current
/// output device configuration.
/// 
/// Output devices that display pixels (e.g. a physical monitor or a virtual
/// output in a window) are represented as heads. Heads cannot be created nor
/// destroyed by the client, but they can be enabled or disabled and their
/// properties can be changed. Each head may have one or more available modes.
/// 
/// Whenever a head appears (e.g. a monitor is plugged in), it will be
/// advertised via the head event. Immediately after the output manager is
/// bound, all current heads are advertised.
/// 
/// Whenever a head's properties change, the relevant wlr_output_head events
/// will be sent. Not all head properties will be sent: only properties that
/// have changed need to.
/// 
/// Whenever a head disappears (e.g. a monitor is unplugged), a
/// wlr_output_head.finished event will be sent.
/// 
/// After one or more heads appear, change or disappear, the done event will
/// be sent. It carries a serial which can be used in a create_configuration
/// request to update heads properties.
/// 
/// The information obtained from this protocol should only be used for output
/// configuration purposes. This protocol is not designed to be a generic
/// output property advertisement protocol for regular clients. Instead,
/// protocols such as xdg-output should be used.
/// 
class OutputManagerV1 extends Proxy implements Dispatcher{
  final Context innerContext;
  final version = 4;

  OutputManagerV1(this.innerContext) : super(innerContext.allocateClientId()){
    innerContext.register(this);
  }


@override
toString(){
return "OutputManagerV1 {name: 'zwlr_output_manager_v1', id: '$objectId', version: '4',}";
}

/// create a new output configuration object
/// 
/// Create a new output configuration object. This allows to update head
/// properties.
/// 
/// [id]:
/// [serial]:
  Result<OutputConfigurationV1,Object> createConfiguration(int serial) {
  var id =  OutputConfigurationV1(innerContext);
    logLn("OutputManagerV1::createConfiguration  id: $id serial: $serial");
var arguments = [id, serial];var argTypes = <WaylandType>[WaylandType.newId, WaylandType.uint];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 0]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([id.objectId]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([serial]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputManagerV1::createConfiguration: $e");
   return Failure(e);
    }
    return Success(id);
  }

/// stop sending events
/// 
/// Indicates the client no longer wishes to receive events for output
/// configuration changes. However the compositor may emit further events,
/// until the finished event is emitted.
/// 
/// The client must not send any more requests after this one.
/// 
  Result<void,Object> stop() {
    logLn("OutputManagerV1::stop ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 1]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputManagerV1::stop: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// introduce a new head
/// 
/// This event introduces a new head. This happens whenever a new head
/// appears (e.g. a monitor is plugged in) or after the output manager is
/// bound.
/// 
/// Event handler for Head
/// - [head]:
 void onHead(OutputManagerV1HeadEventHandler handler) {
   _headHandler = handler;
 }

 OutputManagerV1HeadEventHandler? _headHandler;

/// sent all information about current configuration
/// 
/// This event is sent after all information has been sent after binding to
/// the output manager object and after any subsequent changes. This applies
/// to child head and mode objects as well. In other words, this event is
/// sent whenever a head or mode is created or destroyed and whenever one of
/// their properties has been changed. Not all state is re-sent each time
/// the current configuration changes: only the actual changes are sent.
/// 
/// This allows changes to the output configuration to be seen as atomic,
/// even if they happen via multiple events.
/// 
/// A serial is sent to be used in a future create_configuration request.
/// 
/// Event handler for Done
/// - [serial]: current configuration serial
 void onDone(OutputManagerV1DoneEventHandler handler) {
   _doneHandler = handler;
 }

 OutputManagerV1DoneEventHandler? _doneHandler;

/// the compositor has finished with the manager
/// 
/// This event indicates that the compositor is done sending manager events.
/// The compositor will destroy the object immediately after sending this
/// event, so it will become invalid and the client should release any
/// resources associated with it.
/// 
/// Event handler for Finished
 void onFinished(OutputManagerV1FinishedEventHandler handler) {
   _finishedHandler = handler;
 }

 OutputManagerV1FinishedEventHandler? _finishedHandler;

 @override
 void dispatch(int opcode, int fd, Uint8List data) {
logLn("OutputManagerV1.dispatch($opcode, $fd, $data)");
   switch (opcode) {     case 0:

       if (_headHandler != null) {
var _offset = 0;
final head = innerContext.getProxy(ByteData.view(data.buffer).getUint32(_offset, Endian.little)).objectId;
_offset += 4;
var event = OutputManagerV1HeadEvent(
head,
        );
         _headHandler!(event);
       }
       break;
     case 1:

       if (_doneHandler != null) {
var _offset = 0;
final serial = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
var event = OutputManagerV1DoneEvent(
serial,
        );
         _doneHandler!(event);
       }
       break;
     case 2:

       if (_finishedHandler != null) {
         _finishedHandler!(OutputManagerV1FinishedEvent());
       }
       break;
   }
 }
}


/// head name
/// 
/// This event describes the head name.
/// 
/// The naming convention is compositor defined, but limited to alphanumeric
/// characters and dashes (-). Each name is unique among all wlr_output_head
/// objects, but if a wlr_output_head object is destroyed the same name may
/// be reused later. The names will also remain consistent across sessions
/// with the same hardware and software configuration.
/// 
/// Examples of names include 'HDMI-A-1', 'WL-1', 'X11-1', etc. However, do
/// not assume that the name is a reflection of an underlying DRM
/// connector, X11 connection, etc.
/// 
/// If this head matches a wl_output, the wl_output.name event must report
/// the same name.
/// 
/// The name event is sent after a wlr_output_head object is created. This
/// event is only sent once per object, and the name does not change over
/// the lifetime of the wlr_output_head object.
/// 
class OutputHeadV1NameEvent {
/// 
  final String name;

  OutputHeadV1NameEvent(
this.name,

);

@override
toString(){
return "OutputHeadV1NameEvent (name: $name)";
}
}

typedef OutputHeadV1NameEventHandler = void Function(OutputHeadV1NameEvent);

/// head description
/// 
/// This event describes a human-readable description of the head.
/// 
/// The description is a UTF-8 string with no convention defined for its
/// contents. Examples might include 'Foocorp 11" Display' or 'Virtual X11
/// output via :1'. However, do not assume that the name is a reflection of
/// the make, model, serial of the underlying DRM connector or the display
/// name of the underlying X11 connection, etc.
/// 
/// If this head matches a wl_output, the wl_output.description event must
/// report the same name.
/// 
/// The description event is sent after a wlr_output_head object is created.
/// This event is only sent once per object, and the description does not
/// change over the lifetime of the wlr_output_head object.
/// 
class OutputHeadV1DescriptionEvent {
/// 
  final String description;

  OutputHeadV1DescriptionEvent(
this.description,

);

@override
toString(){
return "OutputHeadV1DescriptionEvent (description: $description)";
}
}

typedef OutputHeadV1DescriptionEventHandler = void Function(OutputHeadV1DescriptionEvent);

/// head physical size
/// 
/// This event describes the physical size of the head. This event is only
/// sent if the head has a physical size (e.g. is not a projector or a
/// virtual device).
/// 
/// The physical size event is sent after a wlr_output_head object is created. This
/// event is only sent once per object, and the physical size does not change over
/// the lifetime of the wlr_output_head object.
/// 
class OutputHeadV1PhysicalSizeEvent {
/// width in millimeters of the output
  final int width;

/// height in millimeters of the output
  final int height;

  OutputHeadV1PhysicalSizeEvent(
this.width,

this.height,

);

@override
toString(){
return "OutputHeadV1PhysicalSizeEvent (width: $width, height: $height)";
}
}

typedef OutputHeadV1PhysicalSizeEventHandler = void Function(OutputHeadV1PhysicalSizeEvent);

/// introduce a mode
/// 
/// This event introduces a mode for this head. It is sent once per
/// supported mode.
/// 
class OutputHeadV1ModeEvent {
/// 
  final int mode;

  OutputHeadV1ModeEvent(
this.mode,

);

@override
toString(){
return "OutputHeadV1ModeEvent (mode: $mode)";
}
}

typedef OutputHeadV1ModeEventHandler = void Function(OutputHeadV1ModeEvent);

/// head is enabled or disabled
/// 
/// This event describes whether the head is enabled. A disabled head is not
/// mapped to a region of the global compositor space.
/// 
/// When a head is disabled, some properties (current_mode, position,
/// transform and scale) are irrelevant.
/// 
class OutputHeadV1EnabledEvent {
/// zero if disabled, non-zero if enabled
  final int enabled;

  OutputHeadV1EnabledEvent(
this.enabled,

);

@override
toString(){
return "OutputHeadV1EnabledEvent (enabled: $enabled)";
}
}

typedef OutputHeadV1EnabledEventHandler = void Function(OutputHeadV1EnabledEvent);

/// current mode
/// 
/// This event describes the mode currently in use for this head. It is only
/// sent if the output is enabled.
/// 
class OutputHeadV1CurrentModeEvent {
/// 
  final int mode;

  OutputHeadV1CurrentModeEvent(
this.mode,

);

@override
toString(){
return "OutputHeadV1CurrentModeEvent (mode: $mode)";
}
}

typedef OutputHeadV1CurrentModeEventHandler = void Function(OutputHeadV1CurrentModeEvent);

/// current position
/// 
/// This events describes the position of the head in the global compositor
/// space. It is only sent if the output is enabled.
/// 
class OutputHeadV1PositionEvent {
/// x position within the global compositor space
  final int x;

/// y position within the global compositor space
  final int y;

  OutputHeadV1PositionEvent(
this.x,

this.y,

);

@override
toString(){
return "OutputHeadV1PositionEvent (x: $x, y: $y)";
}
}

typedef OutputHeadV1PositionEventHandler = void Function(OutputHeadV1PositionEvent);

/// current transformation
/// 
/// This event describes the transformation currently applied to the head.
/// It is only sent if the output is enabled.
/// 
class OutputHeadV1TransformEvent {
/// 
  final int transform;

  OutputHeadV1TransformEvent(
this.transform,

);

@override
toString(){
return "OutputHeadV1TransformEvent (transform: $transform)";
}
}

typedef OutputHeadV1TransformEventHandler = void Function(OutputHeadV1TransformEvent);

/// current scale
/// 
/// This events describes the scale of the head in the global compositor
/// space. It is only sent if the output is enabled.
/// 
class OutputHeadV1ScaleEvent {
/// 
  final double scale;

  OutputHeadV1ScaleEvent(
this.scale,

);

@override
toString(){
return "OutputHeadV1ScaleEvent (scale: $scale)";
}
}

typedef OutputHeadV1ScaleEventHandler = void Function(OutputHeadV1ScaleEvent);

/// the head has disappeared
/// 
/// This event indicates that the head is no longer available. The head
/// object becomes inert. Clients should send a destroy request and release
/// any resources associated with it.
/// 
class OutputHeadV1FinishedEvent {
  OutputHeadV1FinishedEvent(
);

@override
toString(){
return "OutputHeadV1FinishedEvent ()";
}
}

typedef OutputHeadV1FinishedEventHandler = void Function(OutputHeadV1FinishedEvent);

/// head manufacturer
/// 
/// This event describes the manufacturer of the head.
/// 
/// Together with the model and serial_number events the purpose is to
/// allow clients to recognize heads from previous sessions and for example
/// load head-specific configurations back.
/// 
/// It is not guaranteed this event will be ever sent. A reason for that
/// can be that the compositor does not have information about the make of
/// the head or the definition of a make is not sensible in the current
/// setup, for example in a virtual session. Clients can still try to
/// identify the head by available information from other events but should
/// be aware that there is an increased risk of false positives.
/// 
/// If sent, the make event is sent after a wlr_output_head object is
/// created and only sent once per object. The make does not change over
/// the lifetime of the wlr_output_head object.
/// 
/// It is not recommended to display the make string in UI to users. For
/// that the string provided by the description event should be preferred.
/// 
class OutputHeadV1MakeEvent {
/// 
  final String make;

  OutputHeadV1MakeEvent(
this.make,

);

@override
toString(){
return "OutputHeadV1MakeEvent (make: $make)";
}
}

typedef OutputHeadV1MakeEventHandler = void Function(OutputHeadV1MakeEvent);

/// head model
/// 
/// This event describes the model of the head.
/// 
/// Together with the make and serial_number events the purpose is to
/// allow clients to recognize heads from previous sessions and for example
/// load head-specific configurations back.
/// 
/// It is not guaranteed this event will be ever sent. A reason for that
/// can be that the compositor does not have information about the model of
/// the head or the definition of a model is not sensible in the current
/// setup, for example in a virtual session. Clients can still try to
/// identify the head by available information from other events but should
/// be aware that there is an increased risk of false positives.
/// 
/// If sent, the model event is sent after a wlr_output_head object is
/// created and only sent once per object. The model does not change over
/// the lifetime of the wlr_output_head object.
/// 
/// It is not recommended to display the model string in UI to users. For
/// that the string provided by the description event should be preferred.
/// 
class OutputHeadV1ModelEvent {
/// 
  final String model;

  OutputHeadV1ModelEvent(
this.model,

);

@override
toString(){
return "OutputHeadV1ModelEvent (model: $model)";
}
}

typedef OutputHeadV1ModelEventHandler = void Function(OutputHeadV1ModelEvent);

/// head serial number
/// 
/// This event describes the serial number of the head.
/// 
/// Together with the make and model events the purpose is to allow clients
/// to recognize heads from previous sessions and for example load head-
/// specific configurations back.
/// 
/// It is not guaranteed this event will be ever sent. A reason for that
/// can be that the compositor does not have information about the serial
/// number of the head or the definition of a serial number is not sensible
/// in the current setup. Clients can still try to identify the head by
/// available information from other events but should be aware that there
/// is an increased risk of false positives.
/// 
/// If sent, the serial number event is sent after a wlr_output_head object
/// is created and only sent once per object. The serial number does not
/// change over the lifetime of the wlr_output_head object.
/// 
/// It is not recommended to display the serial_number string in UI to
/// users. For that the string provided by the description event should be
/// preferred.
/// 
class OutputHeadV1SerialNumberEvent {
/// 
  final String serialNumber;

  OutputHeadV1SerialNumberEvent(
this.serialNumber,

);

@override
toString(){
return "OutputHeadV1SerialNumberEvent (serialNumber: $serialNumber)";
}
}

typedef OutputHeadV1SerialNumberEventHandler = void Function(OutputHeadV1SerialNumberEvent);

/// current adaptive sync state
/// 
/// This event describes whether adaptive sync is currently enabled for
/// the head or not. Adaptive sync is also known as Variable Refresh
/// Rate or VRR.
/// 
class OutputHeadV1AdaptiveSyncEvent {
/// 
  final int state;

  OutputHeadV1AdaptiveSyncEvent(
this.state,

);

@override
toString(){
return "OutputHeadV1AdaptiveSyncEvent (state: $state)";
}
}

typedef OutputHeadV1AdaptiveSyncEventHandler = void Function(OutputHeadV1AdaptiveSyncEvent);


/// output device
/// 
/// A head is an output device. The difference between a wl_output object and
/// a head is that heads are advertised even if they are turned off. A head
/// object only advertises properties and cannot be used directly to change
/// them.
/// 
/// A head has some read-only properties: modes, name, description and
/// physical_size. These cannot be changed by clients.
/// 
/// Other properties can be updated via a wlr_output_configuration object.
/// 
/// Properties sent via this interface are applied atomically via the
/// wlr_output_manager.done event. No guarantees are made regarding the order
/// in which properties are sent.
/// 
class OutputHeadV1 extends Proxy implements Dispatcher{
  final Context innerContext;
  final version = 4;

  OutputHeadV1(this.innerContext) : super(innerContext.allocateClientId()){
    innerContext.register(this);
  }


@override
toString(){
return "OutputHeadV1 {name: 'zwlr_output_head_v1', id: '$objectId', version: '4',}";
}

/// destroy the head object
/// 
/// This request indicates that the client will no longer use this head
/// object.
/// 
  Result<void,Object> release() {
innerContext.unRegister(this);
    logLn("OutputHeadV1::release ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 0]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputHeadV1::release: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// head name
/// 
/// This event describes the head name.
/// 
/// The naming convention is compositor defined, but limited to alphanumeric
/// characters and dashes (-). Each name is unique among all wlr_output_head
/// objects, but if a wlr_output_head object is destroyed the same name may
/// be reused later. The names will also remain consistent across sessions
/// with the same hardware and software configuration.
/// 
/// Examples of names include 'HDMI-A-1', 'WL-1', 'X11-1', etc. However, do
/// not assume that the name is a reflection of an underlying DRM
/// connector, X11 connection, etc.
/// 
/// If this head matches a wl_output, the wl_output.name event must report
/// the same name.
/// 
/// The name event is sent after a wlr_output_head object is created. This
/// event is only sent once per object, and the name does not change over
/// the lifetime of the wlr_output_head object.
/// 
/// Event handler for Name
/// - [name]:
 void onName(OutputHeadV1NameEventHandler handler) {
   _nameHandler = handler;
 }

 OutputHeadV1NameEventHandler? _nameHandler;

/// head description
/// 
/// This event describes a human-readable description of the head.
/// 
/// The description is a UTF-8 string with no convention defined for its
/// contents. Examples might include 'Foocorp 11" Display' or 'Virtual X11
/// output via :1'. However, do not assume that the name is a reflection of
/// the make, model, serial of the underlying DRM connector or the display
/// name of the underlying X11 connection, etc.
/// 
/// If this head matches a wl_output, the wl_output.description event must
/// report the same name.
/// 
/// The description event is sent after a wlr_output_head object is created.
/// This event is only sent once per object, and the description does not
/// change over the lifetime of the wlr_output_head object.
/// 
/// Event handler for Description
/// - [description]:
 void onDescription(OutputHeadV1DescriptionEventHandler handler) {
   _descriptionHandler = handler;
 }

 OutputHeadV1DescriptionEventHandler? _descriptionHandler;

/// head physical size
/// 
/// This event describes the physical size of the head. This event is only
/// sent if the head has a physical size (e.g. is not a projector or a
/// virtual device).
/// 
/// The physical size event is sent after a wlr_output_head object is created. This
/// event is only sent once per object, and the physical size does not change over
/// the lifetime of the wlr_output_head object.
/// 
/// Event handler for PhysicalSize
/// - [width]: width in millimeters of the output
/// - [height]: height in millimeters of the output
 void onPhysicalSize(OutputHeadV1PhysicalSizeEventHandler handler) {
   _physicalSizeHandler = handler;
 }

 OutputHeadV1PhysicalSizeEventHandler? _physicalSizeHandler;

/// introduce a mode
/// 
/// This event introduces a mode for this head. It is sent once per
/// supported mode.
/// 
/// Event handler for Mode
/// - [mode]:
 void onMode(OutputHeadV1ModeEventHandler handler) {
   _modeHandler = handler;
 }

 OutputHeadV1ModeEventHandler? _modeHandler;

/// head is enabled or disabled
/// 
/// This event describes whether the head is enabled. A disabled head is not
/// mapped to a region of the global compositor space.
/// 
/// When a head is disabled, some properties (current_mode, position,
/// transform and scale) are irrelevant.
/// 
/// Event handler for Enabled
/// - [enabled]: zero if disabled, non-zero if enabled
 void onEnabled(OutputHeadV1EnabledEventHandler handler) {
   _enabledHandler = handler;
 }

 OutputHeadV1EnabledEventHandler? _enabledHandler;

/// current mode
/// 
/// This event describes the mode currently in use for this head. It is only
/// sent if the output is enabled.
/// 
/// Event handler for CurrentMode
/// - [mode]:
 void onCurrentMode(OutputHeadV1CurrentModeEventHandler handler) {
   _currentModeHandler = handler;
 }

 OutputHeadV1CurrentModeEventHandler? _currentModeHandler;

/// current position
/// 
/// This events describes the position of the head in the global compositor
/// space. It is only sent if the output is enabled.
/// 
/// Event handler for Position
/// - [x]: x position within the global compositor space
/// - [y]: y position within the global compositor space
 void onPosition(OutputHeadV1PositionEventHandler handler) {
   _positionHandler = handler;
 }

 OutputHeadV1PositionEventHandler? _positionHandler;

/// current transformation
/// 
/// This event describes the transformation currently applied to the head.
/// It is only sent if the output is enabled.
/// 
/// Event handler for Transform
/// - [transform]:
 void onTransform(OutputHeadV1TransformEventHandler handler) {
   _transformHandler = handler;
 }

 OutputHeadV1TransformEventHandler? _transformHandler;

/// current scale
/// 
/// This events describes the scale of the head in the global compositor
/// space. It is only sent if the output is enabled.
/// 
/// Event handler for Scale
/// - [scale]:
 void onScale(OutputHeadV1ScaleEventHandler handler) {
   _scaleHandler = handler;
 }

 OutputHeadV1ScaleEventHandler? _scaleHandler;

/// the head has disappeared
/// 
/// This event indicates that the head is no longer available. The head
/// object becomes inert. Clients should send a destroy request and release
/// any resources associated with it.
/// 
/// Event handler for Finished
 void onFinished(OutputHeadV1FinishedEventHandler handler) {
   _finishedHandler = handler;
 }

 OutputHeadV1FinishedEventHandler? _finishedHandler;

/// head manufacturer
/// 
/// This event describes the manufacturer of the head.
/// 
/// Together with the model and serial_number events the purpose is to
/// allow clients to recognize heads from previous sessions and for example
/// load head-specific configurations back.
/// 
/// It is not guaranteed this event will be ever sent. A reason for that
/// can be that the compositor does not have information about the make of
/// the head or the definition of a make is not sensible in the current
/// setup, for example in a virtual session. Clients can still try to
/// identify the head by available information from other events but should
/// be aware that there is an increased risk of false positives.
/// 
/// If sent, the make event is sent after a wlr_output_head object is
/// created and only sent once per object. The make does not change over
/// the lifetime of the wlr_output_head object.
/// 
/// It is not recommended to display the make string in UI to users. For
/// that the string provided by the description event should be preferred.
/// 
/// Event handler for Make
/// - [make]:
 void onMake(OutputHeadV1MakeEventHandler handler) {
   _makeHandler = handler;
 }

 OutputHeadV1MakeEventHandler? _makeHandler;

/// head model
/// 
/// This event describes the model of the head.
/// 
/// Together with the make and serial_number events the purpose is to
/// allow clients to recognize heads from previous sessions and for example
/// load head-specific configurations back.
/// 
/// It is not guaranteed this event will be ever sent. A reason for that
/// can be that the compositor does not have information about the model of
/// the head or the definition of a model is not sensible in the current
/// setup, for example in a virtual session. Clients can still try to
/// identify the head by available information from other events but should
/// be aware that there is an increased risk of false positives.
/// 
/// If sent, the model event is sent after a wlr_output_head object is
/// created and only sent once per object. The model does not change over
/// the lifetime of the wlr_output_head object.
/// 
/// It is not recommended to display the model string in UI to users. For
/// that the string provided by the description event should be preferred.
/// 
/// Event handler for Model
/// - [model]:
 void onModel(OutputHeadV1ModelEventHandler handler) {
   _modelHandler = handler;
 }

 OutputHeadV1ModelEventHandler? _modelHandler;

/// head serial number
/// 
/// This event describes the serial number of the head.
/// 
/// Together with the make and model events the purpose is to allow clients
/// to recognize heads from previous sessions and for example load head-
/// specific configurations back.
/// 
/// It is not guaranteed this event will be ever sent. A reason for that
/// can be that the compositor does not have information about the serial
/// number of the head or the definition of a serial number is not sensible
/// in the current setup. Clients can still try to identify the head by
/// available information from other events but should be aware that there
/// is an increased risk of false positives.
/// 
/// If sent, the serial number event is sent after a wlr_output_head object
/// is created and only sent once per object. The serial number does not
/// change over the lifetime of the wlr_output_head object.
/// 
/// It is not recommended to display the serial_number string in UI to
/// users. For that the string provided by the description event should be
/// preferred.
/// 
/// Event handler for SerialNumber
/// - [serial_number]:
 void onSerialNumber(OutputHeadV1SerialNumberEventHandler handler) {
   _serialNumberHandler = handler;
 }

 OutputHeadV1SerialNumberEventHandler? _serialNumberHandler;

/// current adaptive sync state
/// 
/// This event describes whether adaptive sync is currently enabled for
/// the head or not. Adaptive sync is also known as Variable Refresh
/// Rate or VRR.
/// 
/// Event handler for AdaptiveSync
/// - [state]:
 void onAdaptiveSync(OutputHeadV1AdaptiveSyncEventHandler handler) {
   _adaptiveSyncHandler = handler;
 }

 OutputHeadV1AdaptiveSyncEventHandler? _adaptiveSyncHandler;

 @override
 void dispatch(int opcode, int fd, Uint8List data) {
logLn("OutputHeadV1.dispatch($opcode, $fd, $data)");
   switch (opcode) {     case 0:

       if (_nameHandler != null) {
var _offset = 0;
final nameLength = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
final name = utf8.decode(data.sublist(_offset, _offset + nameLength - 1));
_offset += nameLength; // Skip the string bytes and null terminator
while (_offset % 4 != 0) { _offset++; } // Padding
var event = OutputHeadV1NameEvent(
name,
        );
         _nameHandler!(event);
       }
       break;
     case 1:

       if (_descriptionHandler != null) {
var _offset = 0;
final descriptionLength = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
final description = utf8.decode(data.sublist(_offset, _offset + descriptionLength - 1));
_offset += descriptionLength; // Skip the string bytes and null terminator
while (_offset % 4 != 0) { _offset++; } // Padding
var event = OutputHeadV1DescriptionEvent(
description,
        );
         _descriptionHandler!(event);
       }
       break;
     case 2:

       if (_physicalSizeHandler != null) {
var _offset = 0;
final width = ByteData.view(data.buffer).getInt32(_offset, Endian.little);
_offset += 4;
final height = ByteData.view(data.buffer).getInt32(_offset, Endian.little);
_offset += 4;
var event = OutputHeadV1PhysicalSizeEvent(
width,
height,
        );
         _physicalSizeHandler!(event);
       }
       break;
     case 3:

       if (_modeHandler != null) {
var _offset = 0;
final mode = innerContext.getProxy(ByteData.view(data.buffer).getUint32(_offset, Endian.little)).objectId;
_offset += 4;
var event = OutputHeadV1ModeEvent(
mode,
        );
         _modeHandler!(event);
       }
       break;
     case 4:

       if (_enabledHandler != null) {
var _offset = 0;
final enabled = ByteData.view(data.buffer).getInt32(_offset, Endian.little);
_offset += 4;
var event = OutputHeadV1EnabledEvent(
enabled,
        );
         _enabledHandler!(event);
       }
       break;
     case 5:

       if (_currentModeHandler != null) {
var _offset = 0;
final mode = innerContext.getProxy(ByteData.view(data.buffer).getUint32(_offset, Endian.little)).objectId;
_offset += 4;
var event = OutputHeadV1CurrentModeEvent(
mode,
        );
         _currentModeHandler!(event);
       }
       break;
     case 6:

       if (_positionHandler != null) {
var _offset = 0;
final x = ByteData.view(data.buffer).getInt32(_offset, Endian.little);
_offset += 4;
final y = ByteData.view(data.buffer).getInt32(_offset, Endian.little);
_offset += 4;
var event = OutputHeadV1PositionEvent(
x,
y,
        );
         _positionHandler!(event);
       }
       break;
     case 7:

       if (_transformHandler != null) {
var _offset = 0;
final transform = ByteData.view(data.buffer).getInt32(_offset, Endian.little);
_offset += 4;
var event = OutputHeadV1TransformEvent(
transform,
        );
         _transformHandler!(event);
       }
       break;
     case 8:

       if (_scaleHandler != null) {
var _offset = 0;
final scale = fixedToDouble(ByteData.view(data.buffer).getInt32(_offset, Endian.little));
_offset += 4;
var event = OutputHeadV1ScaleEvent(
scale,
        );
         _scaleHandler!(event);
       }
       break;
     case 9:

       if (_finishedHandler != null) {
         _finishedHandler!(OutputHeadV1FinishedEvent());
       }
       break;
     case 10:

       if (_makeHandler != null) {
var _offset = 0;
final makeLength = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
final make = utf8.decode(data.sublist(_offset, _offset + makeLength - 1));
_offset += makeLength; // Skip the string bytes and null terminator
while (_offset % 4 != 0) { _offset++; } // Padding
var event = OutputHeadV1MakeEvent(
make,
        );
         _makeHandler!(event);
       }
       break;
     case 11:

       if (_modelHandler != null) {
var _offset = 0;
final modelLength = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
final model = utf8.decode(data.sublist(_offset, _offset + modelLength - 1));
_offset += modelLength; // Skip the string bytes and null terminator
while (_offset % 4 != 0) { _offset++; } // Padding
var event = OutputHeadV1ModelEvent(
model,
        );
         _modelHandler!(event);
       }
       break;
     case 12:

       if (_serialNumberHandler != null) {
var _offset = 0;
final serialNumberLength = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
final serialNumber = utf8.decode(data.sublist(_offset, _offset + serialNumberLength - 1));
_offset += serialNumberLength; // Skip the string bytes and null terminator
while (_offset % 4 != 0) { _offset++; } // Padding
var event = OutputHeadV1SerialNumberEvent(
serialNumber,
        );
         _serialNumberHandler!(event);
       }
       break;
     case 13:

       if (_adaptiveSyncHandler != null) {
var _offset = 0;
final state = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
var event = OutputHeadV1AdaptiveSyncEvent(
state,
        );
         _adaptiveSyncHandler!(event);
       }
       break;
   }
 }
}

/// 
/// 

enum OutputHeadV1AdaptiveSyncState {
/// adaptive sync is disabled
  disabled("disabled", 0),/// adaptive sync is enabled
  enabled("enabled", 1);
const OutputHeadV1AdaptiveSyncState(this.enumName, this.enumValue);
final int enumValue;
final String enumName;
@override
toString(){
return "OutputHeadV1AdaptiveSyncState {name: $enumName, value: $enumValue}";
}
}


/// mode size
/// 
/// This event describes the mode size. The size is given in physical
/// hardware units of the output device. This is not necessarily the same as
/// the output size in the global compositor space. For instance, the output
/// may be scaled or transformed.
/// 
class OutputModeV1SizeEvent {
/// width of the mode in hardware units
  final int width;

/// height of the mode in hardware units
  final int height;

  OutputModeV1SizeEvent(
this.width,

this.height,

);

@override
toString(){
return "OutputModeV1SizeEvent (width: $width, height: $height)";
}
}

typedef OutputModeV1SizeEventHandler = void Function(OutputModeV1SizeEvent);

/// mode refresh rate
/// 
/// This event describes the mode's fixed vertical refresh rate. It is only
/// sent if the mode has a fixed refresh rate.
/// 
class OutputModeV1RefreshEvent {
/// vertical refresh rate in mHz
  final int refresh;

  OutputModeV1RefreshEvent(
this.refresh,

);

@override
toString(){
return "OutputModeV1RefreshEvent (refresh: $refresh)";
}
}

typedef OutputModeV1RefreshEventHandler = void Function(OutputModeV1RefreshEvent);

/// mode is preferred
/// 
/// This event advertises this mode as preferred.
/// 
class OutputModeV1PreferredEvent {
  OutputModeV1PreferredEvent(
);

@override
toString(){
return "OutputModeV1PreferredEvent ()";
}
}

typedef OutputModeV1PreferredEventHandler = void Function(OutputModeV1PreferredEvent);

/// the mode has disappeared
/// 
/// This event indicates that the mode is no longer available. The mode
/// object becomes inert. Clients should send a destroy request and release
/// any resources associated with it.
/// 
class OutputModeV1FinishedEvent {
  OutputModeV1FinishedEvent(
);

@override
toString(){
return "OutputModeV1FinishedEvent ()";
}
}

typedef OutputModeV1FinishedEventHandler = void Function(OutputModeV1FinishedEvent);


/// output mode
/// 
/// This object describes an output mode.
/// 
/// Some heads don't support output modes, in which case modes won't be
/// advertised.
/// 
/// Properties sent via this interface are applied atomically via the
/// wlr_output_manager.done event. No guarantees are made regarding the order
/// in which properties are sent.
/// 
class OutputModeV1 extends Proxy implements Dispatcher{
  final Context innerContext;
  final version = 3;

  OutputModeV1(this.innerContext) : super(innerContext.allocateClientId()){
    innerContext.register(this);
  }


@override
toString(){
return "OutputModeV1 {name: 'zwlr_output_mode_v1', id: '$objectId', version: '3',}";
}

/// destroy the mode object
/// 
/// This request indicates that the client will no longer use this mode
/// object.
/// 
  Result<void,Object> release() {
innerContext.unRegister(this);
    logLn("OutputModeV1::release ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 0]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputModeV1::release: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// mode size
/// 
/// This event describes the mode size. The size is given in physical
/// hardware units of the output device. This is not necessarily the same as
/// the output size in the global compositor space. For instance, the output
/// may be scaled or transformed.
/// 
/// Event handler for Size
/// - [width]: width of the mode in hardware units
/// - [height]: height of the mode in hardware units
 void onSize(OutputModeV1SizeEventHandler handler) {
   _sizeHandler = handler;
 }

 OutputModeV1SizeEventHandler? _sizeHandler;

/// mode refresh rate
/// 
/// This event describes the mode's fixed vertical refresh rate. It is only
/// sent if the mode has a fixed refresh rate.
/// 
/// Event handler for Refresh
/// - [refresh]: vertical refresh rate in mHz
 void onRefresh(OutputModeV1RefreshEventHandler handler) {
   _refreshHandler = handler;
 }

 OutputModeV1RefreshEventHandler? _refreshHandler;

/// mode is preferred
/// 
/// This event advertises this mode as preferred.
/// 
/// Event handler for Preferred
 void onPreferred(OutputModeV1PreferredEventHandler handler) {
   _preferredHandler = handler;
 }

 OutputModeV1PreferredEventHandler? _preferredHandler;

/// the mode has disappeared
/// 
/// This event indicates that the mode is no longer available. The mode
/// object becomes inert. Clients should send a destroy request and release
/// any resources associated with it.
/// 
/// Event handler for Finished
 void onFinished(OutputModeV1FinishedEventHandler handler) {
   _finishedHandler = handler;
 }

 OutputModeV1FinishedEventHandler? _finishedHandler;

 @override
 void dispatch(int opcode, int fd, Uint8List data) {
logLn("OutputModeV1.dispatch($opcode, $fd, $data)");
   switch (opcode) {     case 0:

       if (_sizeHandler != null) {
var _offset = 0;
final width = ByteData.view(data.buffer).getInt32(_offset, Endian.little);
_offset += 4;
final height = ByteData.view(data.buffer).getInt32(_offset, Endian.little);
_offset += 4;
var event = OutputModeV1SizeEvent(
width,
height,
        );
         _sizeHandler!(event);
       }
       break;
     case 1:

       if (_refreshHandler != null) {
var _offset = 0;
final refresh = ByteData.view(data.buffer).getInt32(_offset, Endian.little);
_offset += 4;
var event = OutputModeV1RefreshEvent(
refresh,
        );
         _refreshHandler!(event);
       }
       break;
     case 2:

       if (_preferredHandler != null) {
         _preferredHandler!(OutputModeV1PreferredEvent());
       }
       break;
     case 3:

       if (_finishedHandler != null) {
         _finishedHandler!(OutputModeV1FinishedEvent());
       }
       break;
   }
 }
}


/// configuration changes succeeded
/// 
/// Sent after the compositor has successfully applied the changes or
/// tested them.
/// 
/// Upon receiving this event, the client should destroy this object.
/// 
/// If the current configuration has changed, events to describe the changes
/// will be sent followed by a wlr_output_manager.done event.
/// 
class OutputConfigurationV1SucceededEvent {
  OutputConfigurationV1SucceededEvent(
);

@override
toString(){
return "OutputConfigurationV1SucceededEvent ()";
}
}

typedef OutputConfigurationV1SucceededEventHandler = void Function(OutputConfigurationV1SucceededEvent);

/// configuration changes failed
/// 
/// Sent if the compositor rejects the changes or failed to apply them. The
/// compositor should revert any changes made by the apply request that
/// triggered this event.
/// 
/// Upon receiving this event, the client should destroy this object.
/// 
class OutputConfigurationV1FailedEvent {
  OutputConfigurationV1FailedEvent(
);

@override
toString(){
return "OutputConfigurationV1FailedEvent ()";
}
}

typedef OutputConfigurationV1FailedEventHandler = void Function(OutputConfigurationV1FailedEvent);

/// configuration has been cancelled
/// 
/// Sent if the compositor cancels the configuration because the state of an
/// output changed and the client has outdated information (e.g. after an
/// output has been hotplugged).
/// 
/// The client can create a new configuration with a newer serial and try
/// again.
/// 
/// Upon receiving this event, the client should destroy this object.
/// 
class OutputConfigurationV1CancelledEvent {
  OutputConfigurationV1CancelledEvent(
);

@override
toString(){
return "OutputConfigurationV1CancelledEvent ()";
}
}

typedef OutputConfigurationV1CancelledEventHandler = void Function(OutputConfigurationV1CancelledEvent);


/// output configuration
/// 
/// This object is used by the client to describe a full output configuration.
/// 
/// First, the client needs to setup the output configuration. Each head can
/// be either enabled (and configured) or disabled. It is a protocol error to
/// send two enable_head or disable_head requests with the same head. It is a
/// protocol error to omit a head in a configuration.
/// 
/// Then, the client can apply or test the configuration. The compositor will
/// then reply with a succeeded, failed or cancelled event. Finally the client
/// should destroy the configuration object.
/// 
class OutputConfigurationV1 extends Proxy implements Dispatcher{
  final Context innerContext;
  final version = 4;

  OutputConfigurationV1(this.innerContext) : super(innerContext.allocateClientId()){
    innerContext.register(this);
  }


@override
toString(){
return "OutputConfigurationV1 {name: 'zwlr_output_configuration_v1', id: '$objectId', version: '4',}";
}

/// enable and configure a head
/// 
/// Enable a head. This request creates a head configuration object that can
/// be used to change the head's properties.
/// 
/// [id]: a new object to configure the head
/// [head]: the head to be enabled
  Result<OutputConfigurationHeadV1,Object> enableHead(OutputHeadV1 head) {
  var id =  OutputConfigurationHeadV1(innerContext);
    logLn("OutputConfigurationV1::enableHead  id: $id head: $head");
var arguments = [id, head];var argTypes = <WaylandType>[WaylandType.newId, WaylandType.object];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 0]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([id.objectId]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([head.objectId]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationV1::enableHead: $e");
   return Failure(e);
    }
    return Success(id);
  }

/// disable a head
/// 
/// Disable a head.
/// 
/// [head]: the head to be disabled
  Result<void,Object> disableHead(OutputHeadV1 head) {
    logLn("OutputConfigurationV1::disableHead  head: $head");
var arguments = [head];var argTypes = <WaylandType>[WaylandType.object];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 1]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([head.objectId]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationV1::disableHead: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// apply the configuration
/// 
/// Apply the new output configuration.
/// 
/// In case the configuration is successfully applied, there is no guarantee
/// that the new output state matches completely the requested
/// configuration. For instance, a compositor might round the scale if it
/// doesn't support fractional scaling.
/// 
/// After this request has been sent, the compositor must respond with an
/// succeeded, failed or cancelled event. Sending a request that isn't the
/// destructor is a protocol error.
/// 
  Result<void,Object> apply() {
    logLn("OutputConfigurationV1::apply ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 2]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationV1::apply: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// test the configuration
/// 
/// Test the new output configuration. The configuration won't be applied,
/// but will only be validated.
/// 
/// Even if the compositor succeeds to test a configuration, applying it may
/// fail.
/// 
/// After this request has been sent, the compositor must respond with an
/// succeeded, failed or cancelled event. Sending a request that isn't the
/// destructor is a protocol error.
/// 
  Result<void,Object> test() {
    logLn("OutputConfigurationV1::test ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 3]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationV1::test: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// destroy the output configuration
/// 
/// Using this request a client can tell the compositor that it is not going
/// to use the configuration object anymore. Any changes to the outputs
/// that have not been applied will be discarded.
/// 
/// This request also destroys wlr_output_configuration_head objects created
/// via this object.
/// 
  Result<void,Object> destroy() {
innerContext.unRegister(this);
    logLn("OutputConfigurationV1::destroy ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 4]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationV1::destroy: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// configuration changes succeeded
/// 
/// Sent after the compositor has successfully applied the changes or
/// tested them.
/// 
/// Upon receiving this event, the client should destroy this object.
/// 
/// If the current configuration has changed, events to describe the changes
/// will be sent followed by a wlr_output_manager.done event.
/// 
/// Event handler for Succeeded
 void onSucceeded(OutputConfigurationV1SucceededEventHandler handler) {
   _succeededHandler = handler;
 }

 OutputConfigurationV1SucceededEventHandler? _succeededHandler;

/// configuration changes failed
/// 
/// Sent if the compositor rejects the changes or failed to apply them. The
/// compositor should revert any changes made by the apply request that
/// triggered this event.
/// 
/// Upon receiving this event, the client should destroy this object.
/// 
/// Event handler for Failed
 void onFailed(OutputConfigurationV1FailedEventHandler handler) {
   _failedHandler = handler;
 }

 OutputConfigurationV1FailedEventHandler? _failedHandler;

/// configuration has been cancelled
/// 
/// Sent if the compositor cancels the configuration because the state of an
/// output changed and the client has outdated information (e.g. after an
/// output has been hotplugged).
/// 
/// The client can create a new configuration with a newer serial and try
/// again.
/// 
/// Upon receiving this event, the client should destroy this object.
/// 
/// Event handler for Cancelled
 void onCancelled(OutputConfigurationV1CancelledEventHandler handler) {
   _cancelledHandler = handler;
 }

 OutputConfigurationV1CancelledEventHandler? _cancelledHandler;

 @override
 void dispatch(int opcode, int fd, Uint8List data) {
logLn("OutputConfigurationV1.dispatch($opcode, $fd, $data)");
   switch (opcode) {     case 0:

       if (_succeededHandler != null) {
         _succeededHandler!(OutputConfigurationV1SucceededEvent());
       }
       break;
     case 1:

       if (_failedHandler != null) {
         _failedHandler!(OutputConfigurationV1FailedEvent());
       }
       break;
     case 2:

       if (_cancelledHandler != null) {
         _cancelledHandler!(OutputConfigurationV1CancelledEvent());
       }
       break;
   }
 }
}

/// 
/// 

enum OutputConfigurationV1Error {
/// head has been configured twice
  alreadyConfiguredHead("already_configured_head", 1),/// head has not been configured
  unconfiguredHead("unconfigured_head", 2),/// request sent after configuration has been applied or tested
  alreadyUsed("already_used", 3);
const OutputConfigurationV1Error(this.enumName, this.enumValue);
final int enumValue;
final String enumName;
@override
toString(){
return "OutputConfigurationV1Error {name: $enumName, value: $enumValue}";
}
}



/// head configuration
/// 
/// This object is used by the client to update a single head's configuration.
/// 
/// It is a protocol error to set the same property twice.
/// 
class OutputConfigurationHeadV1 extends Proxy{
  final Context innerContext;
  final version = 4;

  OutputConfigurationHeadV1(this.innerContext) : super(innerContext.allocateClientId()){
    innerContext.register(this);
  }


@override
toString(){
return "OutputConfigurationHeadV1 {name: 'zwlr_output_configuration_head_v1', id: '$objectId', version: '4',}";
}

/// set the mode
/// 
/// This request sets the head's mode.
/// 
/// [mode]:
  Result<void,Object> setMode(OutputModeV1 mode) {
    logLn("OutputConfigurationHeadV1::setMode  mode: $mode");
var arguments = [mode];var argTypes = <WaylandType>[WaylandType.object];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 0]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([mode.objectId]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationHeadV1::setMode: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// set a custom mode
/// 
/// This request assigns a custom mode to the head. The size is given in
/// physical hardware units of the output device. If set to zero, the
/// refresh rate is unspecified.
/// 
/// It is a protocol error to set both a mode and a custom mode.
/// 
/// [width]: width of the mode in hardware units
/// [height]: height of the mode in hardware units
/// [refresh]: vertical refresh rate in mHz or zero
  Result<void,Object> setCustomMode(int width, int height, int refresh) {
    logLn("OutputConfigurationHeadV1::setCustomMode  width: $width height: $height refresh: $refresh");
var arguments = [width, height, refresh];var argTypes = <WaylandType>[WaylandType.int, WaylandType.int, WaylandType.int];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 1]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([width]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([height]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([refresh]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationHeadV1::setCustomMode: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// set the position
/// 
/// This request sets the head's position in the global compositor space.
/// 
/// [x]: x position in the global compositor space
/// [y]: y position in the global compositor space
  Result<void,Object> setPosition(int x, int y) {
    logLn("OutputConfigurationHeadV1::setPosition  x: $x y: $y");
var arguments = [x, y];var argTypes = <WaylandType>[WaylandType.int, WaylandType.int];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 2]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([x]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([y]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationHeadV1::setPosition: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// set the transform
/// 
/// This request sets the head's transform.
/// 
/// [transform]:
  Result<void,Object> setTransform(int transform) {
    logLn("OutputConfigurationHeadV1::setTransform  transform: $transform");
var arguments = [transform];var argTypes = <WaylandType>[WaylandType.int];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 3]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([transform]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationHeadV1::setTransform: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// set the scale
/// 
/// This request sets the head's scale.
/// 
/// [scale]:
  Result<void,Object> setScale(double scale) {
    logLn("OutputConfigurationHeadV1::setScale  scale: $scale");
var arguments = [scale];var argTypes = <WaylandType>[WaylandType.fixed];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 4]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([doubleToFixed(scale)]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationHeadV1::setScale: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// enable/disable adaptive sync
/// 
/// This request enables/disables adaptive sync. Adaptive sync is also
/// known as Variable Refresh Rate or VRR.
/// 
/// [state]:
  Result<void,Object> setAdaptiveSync(int state) {
    logLn("OutputConfigurationHeadV1::setAdaptiveSync  state: $state");
var arguments = [state];var argTypes = <WaylandType>[WaylandType.uint];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 5]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([state]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in OutputConfigurationHeadV1::setAdaptiveSync: $e");
   return Failure(e);
    }
    return Success(Object());
  }

}

/// 
/// 

enum OutputConfigurationHeadV1Error {
/// property has already been set
  alreadySet("already_set", 1),/// mode doesn't belong to head
  invalidMode("invalid_mode", 2),/// mode is invalid
  invalidCustomMode("invalid_custom_mode", 3),/// transform value outside enum
  invalidTransform("invalid_transform", 4),/// scale negative or zero
  invalidScale("invalid_scale", 5),/// invalid enum value used in the set_adaptive_sync request
  invalidAdaptiveSyncState("invalid_adaptive_sync_state", 6);
const OutputConfigurationHeadV1Error(this.enumName, this.enumValue);
final int enumValue;
final String enumName;
@override
toString(){
return "OutputConfigurationHeadV1Error {name: $enumName, value: $enumValue}";
}
}

