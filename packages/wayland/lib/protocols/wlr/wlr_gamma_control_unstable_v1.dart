// Generated by dart-wayland-scanner
// https://github.com/your-repo/dart-wayland-scanner
// XML file : https://gitlab.freedesktop.org/wlroots/wlr-protocols/-/raw/master/unstable/wlr-gamma-control-unstable-v1.xml
//
// wlr_gamma_control_unstable_v1 Protocol Copyright: 
/// 
/// Copyright © 2015 Giulio camuffo
/// Copyright © 2018 Simon Ser
/// 
/// Permission to use, copy, modify, distribute, and sell this
/// software and its documentation for any purpose is hereby granted
/// without fee, provided that the above copyright notice appear in
/// all copies and that both that copyright notice and this permission
/// notice appear in supporting documentation, and that the name of
/// the copyright holders not be used in advertising or publicity
/// pertaining to distribution of the software without specific,
/// written prior permission.  The copyright holders make no
/// representations about the suitability of this software for any
/// purpose.  It is provided "as is" without express or implied
/// warranty.
/// 
/// THE COPYRIGHT HOLDERS DISCLAIM ALL WARRANTIES WITH REGARD TO THIS
/// SOFTWARE, INCLUDING ALL IMPLIED WARRANTIES OF MERCHANTABILITY AND
/// FITNESS, IN NO EVENT SHALL THE COPYRIGHT HOLDERS BE LIABLE FOR ANY
/// SPECIAL, INDIRECT OR CONSEQUENTIAL DAMAGES OR ANY DAMAGES
/// WHATSOEVER RESULTING FROM LOSS OF USE, DATA OR PROFITS, WHETHER IN
/// AN ACTION OF CONTRACT, NEGLIGENCE OR OTHER TORTIOUS ACTION,
/// ARISING OUT OF OR IN CONNECTION WITH THE USE OR PERFORMANCE OF
/// THIS SOFTWARE.
/// 

library wayland;

import 'package:wayland/wayland.dart';
import 'package:wayland/protocols/wayland.dart';
import 'dart:async';
import 'dart:convert';
import 'dart:typed_data';
import 'package:result_dart/result_dart.dart';


/// manager to create per-output gamma controls
/// 
/// This interface is a manager that allows creating per-output gamma
/// controls.
/// 
class GammaControlManagerV1 extends Proxy{
  final Context innerContext;
  final version = 1;

  GammaControlManagerV1(this.innerContext) : super(innerContext.allocateClientId()){
    innerContext.register(this);
  }


@override
toString(){
return "GammaControlManagerV1 {name: 'zwlr_gamma_control_manager_v1', id: '$objectId', version: '1',}";
}

/// get a gamma control for an output
/// 
/// Create a gamma control that can be used to adjust gamma tables for the
/// provided output.
/// 
/// [id]:
/// [output]:
  Result<GammaControlV1,Object> getGammaControl(WlOutput output) {
  var id =  GammaControlV1(innerContext);
    logLn("GammaControlManagerV1::getGammaControl  id: $id output: $output");
var arguments = [id, output];var argTypes = <WaylandType>[WaylandType.newId, WaylandType.object];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 0]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([id.objectId]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([output.objectId]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in GammaControlManagerV1::getGammaControl: $e");
   return Failure(e);
    }
    return Success(id);
  }

/// destroy the manager
/// 
/// All objects created by the manager will still remain valid, until their
/// appropriate destroy request has been called.
/// 
  Result<void,Object> destroy() {
innerContext.unRegister(this);
    logLn("GammaControlManagerV1::destroy ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 1]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in GammaControlManagerV1::destroy: $e");
   return Failure(e);
    }
    return Success(Object());
  }

}


/// size of gamma ramps
/// 
/// Advertise the size of each gamma ramp.
/// 
/// This event is sent immediately when the gamma control object is created.
/// 
class GammaControlV1GammaSizeEvent {
/// number of elements in a ramp
  final int size;

  GammaControlV1GammaSizeEvent(
this.size,

);

@override
toString(){
return "GammaControlV1GammaSizeEvent (size: $size)";
}
}

typedef GammaControlV1GammaSizeEventHandler = void Function(GammaControlV1GammaSizeEvent);

/// object no longer valid
/// 
/// This event indicates that the gamma control is no longer valid. This
/// can happen for a number of reasons, including:
/// - The output doesn't support gamma tables
/// - Setting the gamma tables failed
/// - Another client already has exclusive gamma control for this output
/// - The compositor has transferred gamma control to another client
/// 
/// Upon receiving this event, the client should destroy this object.
/// 
class GammaControlV1FailedEvent {
  GammaControlV1FailedEvent(
);

@override
toString(){
return "GammaControlV1FailedEvent ()";
}
}

typedef GammaControlV1FailedEventHandler = void Function(GammaControlV1FailedEvent);


/// adjust gamma tables for an output
/// 
/// This interface allows a client to adjust gamma tables for a particular
/// output.
/// 
/// The client will receive the gamma size, and will then be able to set gamma
/// tables. At any time the compositor can send a failed event indicating that
/// this object is no longer valid.
/// 
/// There can only be at most one gamma control object per output, which
/// has exclusive access to this particular output. When the gamma control
/// object is destroyed, the gamma table is restored to its original value.
/// 
class GammaControlV1 extends Proxy implements Dispatcher{
  final Context innerContext;
  final version = 1;

  GammaControlV1(this.innerContext) : super(innerContext.allocateClientId()){
    innerContext.register(this);
  }


@override
toString(){
return "GammaControlV1 {name: 'zwlr_gamma_control_v1', id: '$objectId', version: '1',}";
}

/// set the gamma table
/// 
/// Set the gamma table. The file descriptor can be memory-mapped to provide
/// the raw gamma table, which contains successive gamma ramps for the red,
/// green and blue channels. Each gamma ramp is an array of 16-byte unsigned
/// integers which has the same length as the gamma size.
/// 
/// The file descriptor data must have the same length as three times the
/// gamma size.
/// 
/// [fd]: gamma table file descriptor
  Result<void,Object> setGamma(int fd) {
    logLn("GammaControlV1::setGamma  fd: $fd");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 0]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), fd);
    }catch (e) {
      logLn("Exception in GammaControlV1::setGamma: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// destroy this control
/// 
/// Destroys the gamma control object. If the object is still valid, this
/// restores the original gamma tables.
/// 
  Result<void,Object> destroy() {
innerContext.unRegister(this);
    logLn("GammaControlV1::destroy ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 1]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in GammaControlV1::destroy: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// size of gamma ramps
/// 
/// Advertise the size of each gamma ramp.
/// 
/// This event is sent immediately when the gamma control object is created.
/// 
/// Event handler for GammaSize
/// - [size]: number of elements in a ramp
 void onGammaSize(GammaControlV1GammaSizeEventHandler handler) {
   _gammaSizeHandler = handler;
 }

 GammaControlV1GammaSizeEventHandler? _gammaSizeHandler;

/// object no longer valid
/// 
/// This event indicates that the gamma control is no longer valid. This
/// can happen for a number of reasons, including:
/// - The output doesn't support gamma tables
/// - Setting the gamma tables failed
/// - Another client already has exclusive gamma control for this output
/// - The compositor has transferred gamma control to another client
/// 
/// Upon receiving this event, the client should destroy this object.
/// 
/// Event handler for Failed
 void onFailed(GammaControlV1FailedEventHandler handler) {
   _failedHandler = handler;
 }

 GammaControlV1FailedEventHandler? _failedHandler;

 @override
 void dispatch(int opcode, int fd, Uint8List data) {
logLn("GammaControlV1.dispatch($opcode, $fd, $data)");
   switch (opcode) {     case 0:

       if (_gammaSizeHandler != null) {
var _offset = 0;
final size = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
var event = GammaControlV1GammaSizeEvent(
size,
        );
         _gammaSizeHandler!(event);
       }
       break;
     case 1:

       if (_failedHandler != null) {
         _failedHandler!(GammaControlV1FailedEvent());
       }
       break;
   }
 }
}

/// 
/// 

enum GammaControlV1Error {
/// invalid gamma tables
  invalidGamma("invalid_gamma", 1);
const GammaControlV1Error(this.enumName, this.enumValue);
final int enumValue;
final String enumName;
@override
toString(){
return "GammaControlV1Error {name: $enumName, value: $enumValue}";
}
}

