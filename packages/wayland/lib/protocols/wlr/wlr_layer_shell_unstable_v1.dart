// Generated by dart-wayland-scanner
// https://github.com/your-repo/dart-wayland-scanner
// XML file : https://gitlab.freedesktop.org/wlroots/wlr-protocols/-/raw/master/unstable/wlr-layer-shell-unstable-v1.xml
//
// wlr_layer_shell_unstable_v1 Protocol Copyright: 
/// 
/// Copyright © 2017 Drew DeVault
/// 
/// Permission to use, copy, modify, distribute, and sell this
/// software and its documentation for any purpose is hereby granted
/// without fee, provided that the above copyright notice appear in
/// all copies and that both that copyright notice and this permission
/// notice appear in supporting documentation, and that the name of
/// the copyright holders not be used in advertising or publicity
/// pertaining to distribution of the software without specific,
/// written prior permission.  The copyright holders make no
/// representations about the suitability of this software for any
/// purpose.  It is provided "as is" without express or implied
/// warranty.
/// 
/// THE COPYRIGHT HOLDERS DISCLAIM ALL WARRANTIES WITH REGARD TO THIS
/// SOFTWARE, INCLUDING ALL IMPLIED WARRANTIES OF MERCHANTABILITY AND
/// FITNESS, IN NO EVENT SHALL THE COPYRIGHT HOLDERS BE LIABLE FOR ANY
/// SPECIAL, INDIRECT OR CONSEQUENTIAL DAMAGES OR ANY DAMAGES
/// WHATSOEVER RESULTING FROM LOSS OF USE, DATA OR PROFITS, WHETHER IN
/// AN ACTION OF CONTRACT, NEGLIGENCE OR OTHER TORTIOUS ACTION,
/// ARISING OUT OF OR IN CONNECTION WITH THE USE OR PERFORMANCE OF
/// THIS SOFTWARE.
/// 

library wayland;

import 'package:wayland/wayland.dart';
import 'package:wayland/protocols/wayland.dart';
import 'dart:async';
import 'dart:convert';
import 'dart:typed_data';
import 'package:result_dart/result_dart.dart';


/// create surfaces that are layers of the desktop
/// 
/// Clients can use this interface to assign the surface_layer role to
/// wl_surfaces. Such surfaces are assigned to a "layer" of the output and
/// rendered with a defined z-depth respective to each other. They may also be
/// anchored to the edges and corners of a screen and specify input handling
/// semantics. This interface should be suitable for the implementation of
/// many desktop shell components, and a broad number of other applications
/// that interact with the desktop.
/// 
class LayerShellV1 extends Proxy{
  final Context innerContext;
  final version = 5;

  LayerShellV1(this.innerContext) : super(innerContext.allocateClientId()){
    innerContext.register(this);
  }


@override
toString(){
return "LayerShellV1 {name: 'zwlr_layer_shell_v1', id: '$objectId', version: '5',}";
}

/// create a layer_surface from a surface
/// 
/// Create a layer surface for an existing surface. This assigns the role of
/// layer_surface, or raises a protocol error if another role is already
/// assigned.
/// 
/// Creating a layer surface from a wl_surface which has a buffer attached
/// or committed is a client error, and any attempts by a client to attach
/// or manipulate a buffer prior to the first layer_surface.configure call
/// must also be treated as errors.
/// 
/// After creating a layer_surface object and setting it up, the client
/// must perform an initial commit without any buffer attached.
/// The compositor will reply with a layer_surface.configure event.
/// The client must acknowledge it and is then allowed to attach a buffer
/// to map the surface.
/// 
/// You may pass NULL for output to allow the compositor to decide which
/// output to use. Generally this will be the one that the user most
/// recently interacted with.
/// 
/// Clients can specify a namespace that defines the purpose of the layer
/// surface.
/// 
/// [id]:
/// [surface]:
/// [output]:
/// [layer]: layer to add this surface to
/// [namespace]: namespace for the layer surface
  Result<LayerSurfaceV1,Object> getLayerSurface(WlSurface surface, WlOutput output, int layer, String namespace) {
  var id =  LayerSurfaceV1(innerContext);
    logLn("LayerShellV1::getLayerSurface  id: $id surface: $surface output: $output layer: $layer namespace: $namespace");
var arguments = [id, surface, output, layer, namespace];var argTypes = <WaylandType>[WaylandType.newId, WaylandType.object, WaylandType.object, WaylandType.uint, WaylandType.string];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 0]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([id.objectId]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([surface.objectId]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([output.objectId]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([layer]).buffer.asUint8List());
    final namespaceBytes = utf8.encode(namespace);
    bytesBuilder.add(Uint32List.fromList([namespaceBytes.length + 1]).buffer.asUint8List());
    bytesBuilder.add(namespaceBytes);
    bytesBuilder.add([0]); // Null terminator
    while (bytesBuilder.length % 4 != 0) { bytesBuilder.add([0]); } // Padding
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerShellV1::getLayerSurface: $e");
   return Failure(e);
    }
    return Success(id);
  }

/// destroy the layer_shell object
/// 
/// This request indicates that the client will not use the layer_shell
/// object any more. Objects that have been created through this instance
/// are not affected.
/// 
  Result<void,Object> destroy() {
innerContext.unRegister(this);
    logLn("LayerShellV1::destroy ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 1]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerShellV1::destroy: $e");
   return Failure(e);
    }
    return Success(Object());
  }

}

/// 
/// 

enum LayerShellV1Error {
/// wl_surface has another role
  role("role", 0),/// layer value is invalid
  invalidLayer("invalid_layer", 1),/// wl_surface has a buffer attached or committed
  alreadyConstructed("already_constructed", 2);
const LayerShellV1Error(this.enumName, this.enumValue);
final int enumValue;
final String enumName;
@override
toString(){
return "LayerShellV1Error {name: $enumName, value: $enumValue}";
}
}

/// available layers for surfaces
/// 
/// These values indicate which layers a surface can be rendered in. They
/// are ordered by z depth, bottom-most first. Traditional shell surfaces
/// will typically be rendered between the bottom and top layers.
/// Fullscreen shell surfaces are typically rendered at the top layer.
/// Multiple surfaces can share a single layer, and ordering within a
/// single layer is undefined.
/// 

enum LayerShellV1Layer {
/// 
  background("background", 0),/// 
  bottom("bottom", 1),/// 
  top("top", 2),/// 
  overlay("overlay", 3);
const LayerShellV1Layer(this.enumName, this.enumValue);
final int enumValue;
final String enumName;
@override
toString(){
return "LayerShellV1Layer {name: $enumName, value: $enumValue}";
}
}


/// suggest a surface change
/// 
/// The configure event asks the client to resize its surface.
/// 
/// Clients should arrange their surface for the new states, and then send
/// an ack_configure request with the serial sent in this configure event at
/// some point before committing the new surface.
/// 
/// The client is free to dismiss all but the last configure event it
/// received.
/// 
/// The width and height arguments specify the size of the window in
/// surface-local coordinates.
/// 
/// The size is a hint, in the sense that the client is free to ignore it if
/// it doesn't resize, pick a smaller size (to satisfy aspect ratio or
/// resize in steps of NxM pixels). If the client picks a smaller size and
/// is anchored to two opposite anchors (e.g. 'top' and 'bottom'), the
/// surface will be centered on this axis.
/// 
/// If the width or height arguments are zero, it means the client should
/// decide its own window dimension.
/// 
class LayerSurfaceV1ConfigureEvent {
/// 
  final int serial;

/// 
  final int width;

/// 
  final int height;

  LayerSurfaceV1ConfigureEvent(
this.serial,

this.width,

this.height,

);

@override
toString(){
return "LayerSurfaceV1ConfigureEvent (serial: $serial, width: $width, height: $height)";
}
}

typedef LayerSurfaceV1ConfigureEventHandler = void Function(LayerSurfaceV1ConfigureEvent);

/// surface should be closed
/// 
/// The closed event is sent by the compositor when the surface will no
/// longer be shown. The output may have been destroyed or the user may
/// have asked for it to be removed. Further changes to the surface will be
/// ignored. The client should destroy the resource after receiving this
/// event, and create a new surface if they so choose.
/// 
class LayerSurfaceV1ClosedEvent {
  LayerSurfaceV1ClosedEvent(
);

@override
toString(){
return "LayerSurfaceV1ClosedEvent ()";
}
}

typedef LayerSurfaceV1ClosedEventHandler = void Function(LayerSurfaceV1ClosedEvent);


/// layer metadata interface
/// 
/// An interface that may be implemented by a wl_surface, for surfaces that
/// are designed to be rendered as a layer of a stacked desktop-like
/// environment.
/// 
/// Layer surface state (layer, size, anchor, exclusive zone,
/// margin, interactivity) is double-buffered, and will be applied at the
/// time wl_surface.commit of the corresponding wl_surface is called.
/// 
/// Attaching a null buffer to a layer surface unmaps it.
/// 
/// Unmapping a layer_surface means that the surface cannot be shown by the
/// compositor until it is explicitly mapped again. The layer_surface
/// returns to the state it had right after layer_shell.get_layer_surface.
/// The client can re-map the surface by performing a commit without any
/// buffer attached, waiting for a configure event and handling it as usual.
/// 
class LayerSurfaceV1 extends Proxy implements Dispatcher{
  final Context innerContext;
  final version = 5;

  LayerSurfaceV1(this.innerContext) : super(innerContext.allocateClientId()){
    innerContext.register(this);
  }


@override
toString(){
return "LayerSurfaceV1 {name: 'zwlr_layer_surface_v1', id: '$objectId', version: '5',}";
}

/// sets the size of the surface
/// 
/// Sets the size of the surface in surface-local coordinates. The
/// compositor will display the surface centered with respect to its
/// anchors.
/// 
/// If you pass 0 for either value, the compositor will assign it and
/// inform you of the assignment in the configure event. You must set your
/// anchor to opposite edges in the dimensions you omit; not doing so is a
/// protocol error. Both values are 0 by default.
/// 
/// Size is double-buffered, see wl_surface.commit.
/// 
/// [width]:
/// [height]:
  Result<void,Object> setSize(int width, int height) {
    logLn("LayerSurfaceV1::setSize  width: $width height: $height");
var arguments = [width, height];var argTypes = <WaylandType>[WaylandType.uint, WaylandType.uint];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 0]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([width]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([height]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::setSize: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// configures the anchor point of the surface
/// 
/// Requests that the compositor anchor the surface to the specified edges
/// and corners. If two orthogonal edges are specified (e.g. 'top' and
/// 'left'), then the anchor point will be the intersection of the edges
/// (e.g. the top left corner of the output); otherwise the anchor point
/// will be centered on that edge, or in the center if none is specified.
/// 
/// Anchor is double-buffered, see wl_surface.commit.
/// 
/// [anchor]:
  Result<void,Object> setAnchor(int anchor) {
    logLn("LayerSurfaceV1::setAnchor  anchor: $anchor");
var arguments = [anchor];var argTypes = <WaylandType>[WaylandType.uint];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 1]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([anchor]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::setAnchor: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// configures the exclusive geometry of this surface
/// 
/// Requests that the compositor avoids occluding an area with other
/// surfaces. The compositor's use of this information is
/// implementation-dependent - do not assume that this region will not
/// actually be occluded.
/// 
/// A positive value is only meaningful if the surface is anchored to one
/// edge or an edge and both perpendicular edges. If the surface is not
/// anchored, anchored to only two perpendicular edges (a corner), anchored
/// to only two parallel edges or anchored to all edges, a positive value
/// will be treated the same as zero.
/// 
/// A positive zone is the distance from the edge in surface-local
/// coordinates to consider exclusive.
/// 
/// Surfaces that do not wish to have an exclusive zone may instead specify
/// how they should interact with surfaces that do. If set to zero, the
/// surface indicates that it would like to be moved to avoid occluding
/// surfaces with a positive exclusive zone. If set to -1, the surface
/// indicates that it would not like to be moved to accommodate for other
/// surfaces, and the compositor should extend it all the way to the edges
/// it is anchored to.
/// 
/// For example, a panel might set its exclusive zone to 10, so that
/// maximized shell surfaces are not shown on top of it. A notification
/// might set its exclusive zone to 0, so that it is moved to avoid
/// occluding the panel, but shell surfaces are shown underneath it. A
/// wallpaper or lock screen might set their exclusive zone to -1, so that
/// they stretch below or over the panel.
/// 
/// The default value is 0.
/// 
/// Exclusive zone is double-buffered, see wl_surface.commit.
/// 
/// [zone]:
  Result<void,Object> setExclusiveZone(int zone) {
    logLn("LayerSurfaceV1::setExclusiveZone  zone: $zone");
var arguments = [zone];var argTypes = <WaylandType>[WaylandType.int];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 2]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([zone]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::setExclusiveZone: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// sets a margin from the anchor point
/// 
/// Requests that the surface be placed some distance away from the anchor
/// point on the output, in surface-local coordinates. Setting this value
/// for edges you are not anchored to has no effect.
/// 
/// The exclusive zone includes the margin.
/// 
/// Margin is double-buffered, see wl_surface.commit.
/// 
/// [top]:
/// [right]:
/// [bottom]:
/// [left]:
  Result<void,Object> setMargin(int top, int right, int bottom, int left) {
    logLn("LayerSurfaceV1::setMargin  top: $top right: $right bottom: $bottom left: $left");
var arguments = [top, right, bottom, left];var argTypes = <WaylandType>[WaylandType.int, WaylandType.int, WaylandType.int, WaylandType.int];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 3]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([top]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([right]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([bottom]).buffer.asUint8List());
    bytesBuilder.add(Int32List.fromList([left]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::setMargin: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// requests keyboard events
/// 
/// Set how keyboard events are delivered to this surface. By default,
/// layer shell surfaces do not receive keyboard events; this request can
/// be used to change this.
/// 
/// This setting is inherited by child surfaces set by the get_popup
/// request.
/// 
/// Layer surfaces receive pointer, touch, and tablet events normally. If
/// you do not want to receive them, set the input region on your surface
/// to an empty region.
/// 
/// Keyboard interactivity is double-buffered, see wl_surface.commit.
/// 
/// [keyboard_interactivity]:
  Result<void,Object> setKeyboardInteractivity(int keyboardInteractivity) {
    logLn("LayerSurfaceV1::setKeyboardInteractivity  keyboardInteractivity: $keyboardInteractivity");
var arguments = [keyboardInteractivity];var argTypes = <WaylandType>[WaylandType.uint];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 4]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([keyboardInteractivity]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::setKeyboardInteractivity: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// assign this layer_surface as an xdg_popup parent
/// 
/// This assigns an xdg_popup's parent to this layer_surface.  This popup
/// should have been created via xdg_surface::get_popup with the parent set
/// to NULL, and this request must be invoked before committing the popup's
/// initial state.
/// 
/// See the documentation of xdg_popup for more details about what an
/// xdg_popup is and how it is used.
/// 
/// [popup]:
  Result<void,Object> getPopup(XdgPopup popup) {
    logLn("LayerSurfaceV1::getPopup  popup: $popup");
var arguments = [popup];var argTypes = <WaylandType>[WaylandType.object];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 5]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([popup.objectId]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::getPopup: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// ack a configure event
/// 
/// When a configure event is received, if a client commits the
/// surface in response to the configure event, then the client
/// must make an ack_configure request sometime before the commit
/// request, passing along the serial of the configure event.
/// 
/// If the client receives multiple configure events before it
/// can respond to one, it only has to ack the last configure event.
/// 
/// A client is not required to commit immediately after sending
/// an ack_configure request - it may even ack_configure several times
/// before its next surface commit.
/// 
/// A client may send multiple ack_configure requests before committing, but
/// only the last request sent before a commit indicates which configure
/// event the client really is responding to.
/// 
/// [serial]: the serial from the configure event
  Result<void,Object> ackConfigure(int serial) {
    logLn("LayerSurfaceV1::ackConfigure  serial: $serial");
var arguments = [serial];var argTypes = <WaylandType>[WaylandType.uint];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 6]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([serial]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::ackConfigure: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// destroy the layer_surface
/// 
/// This request destroys the layer surface.
/// 
  Result<void,Object> destroy() {
innerContext.unRegister(this);
    logLn("LayerSurfaceV1::destroy ");
var arguments = [];var argTypes = <WaylandType>[];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 7]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::destroy: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// change the layer of the surface
/// 
/// Change the layer that the surface is rendered on.
/// 
/// Layer is double-buffered, see wl_surface.commit.
/// 
/// [layer]: layer to move this surface to
  Result<void,Object> setLayer(int layer) {
    logLn("LayerSurfaceV1::setLayer  layer: $layer");
var arguments = [layer];var argTypes = <WaylandType>[WaylandType.uint];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 8]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([layer]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::setLayer: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// set the edge the exclusive zone will be applied to
/// 
/// Requests an edge for the exclusive zone to apply. The exclusive
/// edge will be automatically deduced from anchor points when possible,
/// but when the surface is anchored to a corner, it will be necessary
/// to set it explicitly to disambiguate, as it is not possible to deduce
/// which one of the two corner edges should be used.
/// 
/// The edge must be one the surface is anchored to, otherwise the
/// invalid_exclusive_edge protocol error will be raised.
/// 
/// [edge]:
  Result<void,Object> setExclusiveEdge(int edge) {
    logLn("LayerSurfaceV1::setExclusiveEdge  edge: $edge");
var arguments = [edge];var argTypes = <WaylandType>[WaylandType.uint];
var calclulatedSize  = calculateSize(argTypes, arguments);
final bytesBuilder = BytesBuilder();
bytesBuilder.add(Uint32List.fromList([objectId, (calclulatedSize << 16) | 9]).buffer.asUint8List());
    bytesBuilder.add(Uint32List.fromList([edge]).buffer.asUint8List());
    try{
    innerContext.sendMessage(bytesBuilder.toBytes(), );
    }catch (e) {
      logLn("Exception in LayerSurfaceV1::setExclusiveEdge: $e");
   return Failure(e);
    }
    return Success(Object());
  }

/// suggest a surface change
/// 
/// The configure event asks the client to resize its surface.
/// 
/// Clients should arrange their surface for the new states, and then send
/// an ack_configure request with the serial sent in this configure event at
/// some point before committing the new surface.
/// 
/// The client is free to dismiss all but the last configure event it
/// received.
/// 
/// The width and height arguments specify the size of the window in
/// surface-local coordinates.
/// 
/// The size is a hint, in the sense that the client is free to ignore it if
/// it doesn't resize, pick a smaller size (to satisfy aspect ratio or
/// resize in steps of NxM pixels). If the client picks a smaller size and
/// is anchored to two opposite anchors (e.g. 'top' and 'bottom'), the
/// surface will be centered on this axis.
/// 
/// If the width or height arguments are zero, it means the client should
/// decide its own window dimension.
/// 
/// Event handler for Configure
/// - [serial]:
/// - [width]:
/// - [height]:
 void onConfigure(LayerSurfaceV1ConfigureEventHandler handler) {
   _configureHandler = handler;
 }

 LayerSurfaceV1ConfigureEventHandler? _configureHandler;

/// surface should be closed
/// 
/// The closed event is sent by the compositor when the surface will no
/// longer be shown. The output may have been destroyed or the user may
/// have asked for it to be removed. Further changes to the surface will be
/// ignored. The client should destroy the resource after receiving this
/// event, and create a new surface if they so choose.
/// 
/// Event handler for Closed
 void onClosed(LayerSurfaceV1ClosedEventHandler handler) {
   _closedHandler = handler;
 }

 LayerSurfaceV1ClosedEventHandler? _closedHandler;

 @override
 void dispatch(int opcode, int fd, Uint8List data) {
logLn("LayerSurfaceV1.dispatch($opcode, $fd, $data)");
   switch (opcode) {     case 0:

       if (_configureHandler != null) {
var _offset = 0;
final serial = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
final width = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
final height = ByteData.view(data.buffer).getUint32(_offset, Endian.little);
_offset += 4;
var event = LayerSurfaceV1ConfigureEvent(
serial,
width,
height,
        );
         _configureHandler!(event);
       }
       break;
     case 1:

       if (_closedHandler != null) {
         _closedHandler!(LayerSurfaceV1ClosedEvent());
       }
       break;
   }
 }
}

/// types of keyboard interaction possible for a layer shell surface
/// 
/// Types of keyboard interaction possible for layer shell surfaces. The
/// rationale for this is twofold: (1) some applications are not interested
/// in keyboard events and not allowing them to be focused can improve the
/// desktop experience; (2) some applications will want to take exclusive
/// keyboard focus.
/// 

enum LayerSurfaceV1KeyboardInteractivity {
/// 
  none("none", 0),/// 
  exclusive("exclusive", 1),/// 
  onDemand("on_demand", 2);
const LayerSurfaceV1KeyboardInteractivity(this.enumName, this.enumValue);
final int enumValue;
final String enumName;
@override
toString(){
return "LayerSurfaceV1KeyboardInteractivity {name: $enumName, value: $enumValue}";
}
}

/// 
/// 

enum LayerSurfaceV1Error {
/// provided surface state is invalid
  invalidSurfaceState("invalid_surface_state", 0),/// size is invalid
  invalidSize("invalid_size", 1),/// anchor bitfield is invalid
  invalidAnchor("invalid_anchor", 2),/// keyboard interactivity is invalid
  invalidKeyboardInteractivity("invalid_keyboard_interactivity", 3),/// exclusive edge is invalid given the surface anchors
  invalidExclusiveEdge("invalid_exclusive_edge", 4);
const LayerSurfaceV1Error(this.enumName, this.enumValue);
final int enumValue;
final String enumName;
@override
toString(){
return "LayerSurfaceV1Error {name: $enumName, value: $enumValue}";
}
}

/// 
/// 

enum LayerSurfaceV1Anchor {
/// the top edge of the anchor rectangle
  top("top", 1),/// the bottom edge of the anchor rectangle
  bottom("bottom", 2),/// the left edge of the anchor rectangle
  left("left", 4),/// the right edge of the anchor rectangle
  right("right", 8);
const LayerSurfaceV1Anchor(this.enumName, this.enumValue);
final int enumValue;
final String enumName;
@override
toString(){
return "LayerSurfaceV1Anchor {name: $enumName, value: $enumValue}";
}
}

