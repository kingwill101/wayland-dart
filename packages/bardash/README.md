# bardash

A customizable Wayland desktop bar built with `window_toolkit` and Lua
configuration. Bardash provides modules for workspaces, system metrics,
audio/MPRIS, network status, notifications, tray items, and speed testing.

This package is currently an application package and remains private to the
wayland-dart workspace. It is included here so the repository has complete
release metadata when the application is distributed separately.
